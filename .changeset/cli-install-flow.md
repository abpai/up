---
'@andypai/up': minor
---

Add `up --version`, a non-interactive `up setup` flow (`--token`, `--api`, `--app`, `--mode`, `--open/--no-open`, `--yes`) that also prefills the interactive prompts, and store `~/.up/config.toml` with `0600` permissions.
