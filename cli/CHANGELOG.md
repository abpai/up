# @andypai/up

## 0.3.0

### Minor Changes

- 0437e62: Add `up --version`, a non-interactive `up setup` flow (`--token`, `--api`, `--app`, `--mode`, `--open/--no-open`, `--yes`) that also prefills the interactive prompts, and store `~/.up/config.toml` with `0600` permissions.
